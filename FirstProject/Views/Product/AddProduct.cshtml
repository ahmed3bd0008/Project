@model AddProduct;
@{
           @* var  Catogory= ViewData["Catogories"] as List<Catogry> ; *@
            var Catogories= ViewBag.Catogories?? new List<Catogry>();
}
<form method="post" asp-action="AddProduct" asp-controller="Product">
             <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                        <Label asp-for="Name"></Label>
                        <input  class="form-control" asp-for="Name"/>
            </div>
            <div class="form-group">
                        <Label asp-for="Describtion"></Label>
                        <input  class="form-control" asp-for="Describtion"/>
            </div>
            <div class="form-group">
                        <Label asp-for="Price"></Label>
                        <input  class="form-control" asp-for="Price"/>
            </div>
            @* <div class="form-group">
                        <Label asp-for="CategoryID"></Label>
                        <select  class="form-control" asp-for="CategoryID" asp-items="@(new SelectList(Catogory,"Id","Name"))"></select>
            </div> *@
              <div class="form-group">
                        <Label asp-for="CategoryID"></Label>
                        <select  class="form-control" asp-for="CategoryID" asp-items="@(new SelectList(Catogories,"Id","Name"))">
                                    <option>Choose</option>
                        </select>
            </div>
             <div class="form-group">
                        <input  type="submit" value="Add Product" class="btn btn-success"/>
            </div>
</form>